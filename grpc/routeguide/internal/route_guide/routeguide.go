package routeguide

/*
type RouteGuide struct {
	mu sync.RWMutex

	//Dir           string
	feature        *feature
	point *point
	//point      []*segment
}

func (f *feature) GetFeature() (*api.Feature, error) {
	//l.mu.RLock()
	//defer l.mu.RUnlock()

	// Find the feature
	for _, feature := range f.savedFeatures {
		if proto.Equal(feature.Location, f.point) {
			return feature, nil
		}
	}
	// No feature was found, return an unnamed feature
	return &api.Feature{Location: f.point}, nil
}*/
